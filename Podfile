# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'chara' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for chara
  pod 'Firebase'
  pod 'Firebase/Database'
  pod 'FirebaseAuth'
  pod 'Firebase/Storage'
  pod 'IQKeyboardManagerSwift', '4.0.6'
  pod 'Alamofire', '~> 4.3'
  pod 'SWXMLHash', '~> 3.0.0'
  pod "Dropper"

  target 'charaTests' do
    inherit! :search_paths
    # Pods for testing
      pod 'Firebase'
      pod 'Firebase/Database'
      pod 'FirebaseAuth'
  end

  target 'charaUITests' do
    inherit! :search_paths
    # Pods for testing
      pod 'Firebase'
      pod 'Firebase/Database'
      pod 'FirebaseAuth'
  end

end
